<div class="container">
  <table>
    <tr>
      <th>Username</th>
      <th>Email</th>
    </tr>
    <% @users.each do |u| %>
    <tr>
      <td><%= u.username %></td>
      <td><%= u.email %></td>
    </tr>
    <% end %>
  </table>
  <%= link_to 'Register', signup_path %>
</div>